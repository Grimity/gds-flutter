name: gds_tokens
version: 0.0.1
publish_to: none
description: Grimity Design System tokens package

environment:
  sdk: ^3.9.2
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0