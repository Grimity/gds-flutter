import 'package:flutter/widgets.dart';

class GdsModal extends StatelessWidget {
  const GdsModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
